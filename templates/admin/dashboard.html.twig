{% extends '@EasyAdmin/layout.html.twig' %}



{% block main %}

<h2>Créer</h2>
    <div class="actions-container">
        <a href="{{path('admin_product_new')}}" class="btn btn-primary"><i class="fa-solid fa-plus"></i> Nouveau produit</a>
        <a href="{{path('admin_newsletter_new')}}" class="btn btn-primary"><i class="fa-solid fa-plus"></i> Créer newsletter</a>
    </div>
<h2>Commandes</h2>
    <div class="orders-container">
        <a href="{{path('admin_pendingOrders')}}" class="btn btn-info"> Commandes en attente</a>
    </div>
<h2>Stats</h2>
    <div class="stats-container">
        <a href="{{path('admin_charts_products-chart')}}" class="btn btn-warning"><i class="fa fa-bar-chart"></i> Stats</a>
    </div>

{% include 'admin/fields/pending-orders_list.html.twig' %}

<style>
 .actions-container,.orders-container,.stats-container{
        display: flex;
        gap:4rem;
        margin:2rem 0 4rem 0;
    }

    h2{
        border-bottom:var(--white) 0.5px solid;
    }

</style>

{% endblock main %}

